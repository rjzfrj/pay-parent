<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.roncoo.pay.permission.dao.impl.PmsMenuRoleDaoImpl">

	<sql id="pms_menu_role"> PMS_MENU_ROLE </sql>

<!-- 	<insert id="insert" parameterType="com.roncoo.pay.permission.entity.PmsMenuRole">
		<selectKey keyProperty="id" resultType="java.lang.Long" order="BEFORE">
			select SQC_PMS_MENU_ROLE.NEXTVAL AS ID FROM DUAL
		</selectKey>
		INSERT INTO
		<include refid="pms_menu_role" />
		(ID,VER,STATUS,CREATER,CREATE_TIME, EDITOR, EDIT_TIME, REMARK,
		ROLE_ID,MENU_ID
		) values
		( #{id},0,#{status}, #{creater},#{createTime}, #{editor},#{editTime},#{remark},
		#{roleId,jdbcType=INTEGER},
		#{menuId,jdbcType=INTEGER}
		)
	</insert> -->
	
	
	<insert id="insert" parameterType="com.roncoo.pay.permission.entity.PmsMenuRole" useGeneratedKeys="true" keyProperty="id">
		<selectKey keyProperty="id" resultType="java.lang.Long" order="BEFORE">
			SELECT  SQC_PMS_MENU_ROLE.NEXTVAL AS ID FROM DUAL
		</selectKey>
    insert into PMS_MENU_ROLE (ID, VER, CREATER, 
      CREATE_TIME, EDITOR, EDIT_TIME, 
      STATUS, REMARK, ROLE_ID, 
      MENU_ID)
    values (#{id,jdbcType=DECIMAL}, #{ver,jdbcType=DECIMAL}, #{creater,jdbcType=VARCHAR}, 
      #{createTime,jdbcType=TIMESTAMP}, #{editor,jdbcType=VARCHAR}, #{editTime,jdbcType=TIMESTAMP}, 
      #{status,jdbcType=VARCHAR}, #{remark,jdbcType=VARCHAR}, #{roleId,jdbcType=DECIMAL}, 
      #{menuId,jdbcType=DECIMAL})
  </insert>

	<delete id="deleteById" parameterType="java.util.Map">
		DELETE FROM
		<include refid="pms_menu_role" />
		WHERE ID = #{id}
	</delete>

	<select id="selectByPrimaryKey" parameterType="string" resultType="com.roncoo.pay.permission.entity.PmsMenuRole">
		select
		d.id,d.ver,d.status,d.creater,
		d.create_time as "createTime", d.editor,
		d.edit_time as "editTime", d.remark,
		d.role_id as "roleId",
		d.menu_id as "menuId"
		from
		<include refid="pms_menu_role" />
		d
		where d.id = #{id}
	</select>

	<select id="listByRoleId" parameterType="long" resultType="com.roncoo.pay.permission.entity.PmsMenuRole">
		select d.id as "id",
		d.role_id as "roleId",
		d.menu_id as "menuId"
		from
		<include refid="pms_menu_role" />
		d
		where role_id = #{roleId}
	</select>

	<select id="listPage" parameterType="java.util.Map" resultType="com.roncoo.pay.permission.entity.PmsMenuRole">
		select d.id as "id",
		d.role_id as "roleId",
		d.menu_id as "menuId"
		from
		<include refid="pms_menu_role" />
		d
		order by id desc
	</select>

	<select id="listPageCount" parameterType="java.util.Map" resultType="long">
		SELECT COUNT(1) FROM
		<include refid="pms_menu_role" />
		D
	</select>

	<delete id="deleteByRoleId" parameterType="long">
		delete from
		<include refid="pms_menu_role" />
		where role_id = #{roleId}
	</delete>

</mapper>
